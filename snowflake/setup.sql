CREATE OR REPLACE DATABASE PACKVOTE_DB;
USE DATABASE PACKVOTE_DB;

CREATE OR REPLACE WAREHOUSE PACKVOTE_WH
WAREHOUSE_SIZE = 'XSMALL'
AUTO_SUSPEND = 60
AUTO_RESUME = TRUE;

CREATE OR REPLACE SCHEMA CORE;

CREATE OR REPLACE TABLE CORE.GROUP_PREFERENCES (
    USER_ID INT AUTOINCREMENT,
    BUDGET FLOAT,
    DESTINATION STRING,
    DURATION INT,
    TRAVEL_STYLE STRING,
    SHOPPING_INTEREST STRING
);

select * from core.group_preferences;